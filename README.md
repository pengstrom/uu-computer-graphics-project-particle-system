# Computer Graphics Project: Particle System
Implementation of a particle system in OpenGL and C++

## Folders

Look in `src` for the source code. Run `build.sh` to compile and run.

The report is found in `report`.
